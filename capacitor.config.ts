import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.ridho.dompetku',
  appName: 'Dompetku',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
